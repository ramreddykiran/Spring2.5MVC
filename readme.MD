spring mvc demo application using Spring 2.5.6 without annotations
after cloning the application, 
mvn eclipse:eclipse

for running the app, on the terminal
mvn jetty:run

http://localhost:8080/spring2/welcome.htm

